{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "input=$(cat); if echo \"$input\" | grep -Eq 'git (add|commit|push)' && echo \"$input\" | grep -q '\\.env'; then echo '已拦截：检测到对 .env 的 git 操作，密钥文件禁止入库' >&2; exit 2; fi"
          }
        ]
      }
    ]
  }
}
