# Spec · 检索优化（top 3 + 打分修正）

## 模块

新建 `retrieval.py`，从 `faq_demo.py` 中接管检索职责。`faq_demo.py` 导入使用，不再自带检索逻辑。

## 接口

```python
def score(question: str, faq: dict) -> int
    # 从 question 和 faq["q"] 中各提取有效字符集合（汉字 + 数字，正则 [一-鿿0-9]）
    # 返回两个集合的交集大小。标点、字母、空白一律不计分。

def get_top3(question: str, faqs: list[dict]) -> list[dict]
    # 按 score 降序排序，返回前 3 条（不足 3 条返回全部）
    # 若最高分为 0，返回空列表 []（表示无相关政策）

def format_refs(items: list[dict]) -> str
    # 把若干条 FAQ 拼成给模型的参考资料文本块
```

## 行为规则

1. **打分**：只统计汉字和数字的重合，标点/字母/空白不计入。
2. **排序**：分数相同的条目，保持 faq.jsonl 中的原有顺序（稳定排序）。
3. **拒答阈值**：最高分为 0 视为检索失败，返回空列表；主程序输出"抱歉，暂时没有查到相关政策"，不调用模型。
4. **prompt 组装**：top 3 全部作为参考资料拼进 system prompt，回答末尾逐条注明来源文件。

## 验收用例表

| 编号 | 输入 | 预期 |
|---|---|---|
| UC-01 | "公积金个人住房贷款最高能贷多少" | top1 = 最高额度那条（src 含"最高额度"） |
| UC-02 | "二孩家庭额度能上浮吗" | top1 = 二孩支持政策那条（src 含"二孩"） |
| UC-03 | "今天天气怎么样" | get_top3 返回 []（失败态：拒答） |
| UC-04 | "？？？!!！" | 全部为 0 分 → 返回 []（标点不计分） |
| UC-05 | "公积金贷款额度" | 返回 ≤3 条，按分数降序，首条分数 ≥ 尾条分数 |

## 失败态说明

- FAQ 库为空 / 全部 0 分：主程序拒答，不调用 API（不浪费 token）。
- faq.jsonl 某行格式坏：导入时跳过该行并打印警告（沿用现状即可）。

## System Ready 清单

- [ ] 主路径可演示：`python faq_demo.py` 问"我能贷多少"，回答带出处（待跑一次真实 API 验收）
- [x] 失败态有说明：无关问题明确拒答
- [x] 测试可重复验收：`python test_retrieve.py` 全绿（7/7）
- [x] 无未关闭的"以后再说"：延后项登记在 docs/backlog.md
